package com.dld.coupon.util;

public enum Tag {
    IMG_CHANGED, IMG_RECYLED,
    // {
    // Tag[] arrayOfTag = new Tag[2];
    // arrayOfTag[0] = IMG_CHANGED;
    // arrayOfTag[1] = IMG_RECYLED;
    // // ENUM$VALUES = arrayOfTag;
    // }
}
